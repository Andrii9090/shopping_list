import React from "react"
import { View } from "react-native"

export const Line = () => {
    return (
        <View
            style={{
                height: 1,
                backgroundColor: 'rgba(0, 0, 0, .2)',
                margin: 5,
            }}
        ></View>
    )
}